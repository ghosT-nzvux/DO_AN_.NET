<Window x:Class="DO_AN_QLKS.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Đăng nhập" Height="420" Width="640"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#F3F4F6">
    <Grid>
        <Border HorizontalAlignment="Center" VerticalAlignment="Center"
                Width="420" Padding="24" Background="White"
                BorderBrush="#DDDDDD" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="Đăng nhập hệ thống" FontSize="20"
                           FontWeight="SemiBold" Margin="0,0,0,12"/>
                <Separator/>
                <TextBlock Text="Tên đăng nhập" Margin="0,8,0,2"/>
                <TextBox x:Name="txtUsername" Margin="0,0,0,8" Padding="8,6"/>
                <TextBlock Text="Mật khẩu" Margin="0,4,0,2"/>
                <PasswordBox x:Name="txtPassword" Margin="0,0,0,12" Padding="8,6"/>
                <DockPanel>

                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                        <Button x:Name="btnExit" Content="Thoát" Width="100" Margin="8,0,0,0" Click="BtnExit_Click"/>
                        <Button x:Name="btnLogin" Content="Đăng nhập" Width="110" Click="BtnLogin_Click"/>
                    </StackPanel>
                </DockPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>

